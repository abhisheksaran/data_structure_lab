#include<stdio.h>
#include<stdlib.h>

int* pivotPartition(int a[],int n)
{
	for(int i=0;i<n;i++)
	{
		if(a[i]<)
	}
}

int findPivotPosition(int a[],int n)
{
	int i;
	int count=0;
	for(i=1;i<n;i++)
	{
	if(a[i]<=a[0])
	count++;	
	}
	return count;
}
int main()
{
	int i;
	int array[]={7,5,4,67,6,5,3,7,9,0};
	int n=sizeof(array)/sizeof(array[0]);

	int pivPos=findPivotPosition(array,n);
	int pivot=array[j]
	printf("%d\n",pivPos);

return 0;
}

